{
  "name": "not-substance-painter",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --serve",
    "check": "tsc --noEmit",
    "blender:dat-to-svg": "node scripts/blender-dat-to-svg.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.27.4",
    "typescript": "^6.0.2"
  }
}
